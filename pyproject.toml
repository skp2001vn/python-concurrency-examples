[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "python-concurrency-examples"
version = "0.1.0"
description = "Small, focused Python concurrency examples."
readme = "README.md"
requires-python = ">=3.9"
